export const dateFormatter = new Intl.DateTimeFormat("bg", {
  year: "numeric",
  month: "long",
  day: "numeric",
});

export const timeFormatter = new Intl.DateTimeFormat("bg", {
  hour: "numeric",
  minute: "numeric",
});
